# CashBox - 캐시 리워드 앱 디자인 계획

## 앱 개요
틱톡 라이트 스타일의 캐시 리워드 모바일 앱. 사용자가 캐시를 적립하고 출금, 기프트카드, 기프트콘으로 사용할 수 있는 앱입니다.

## 화면 구조 (Screen List)

### 1. 인증 화면
- **로그인/회원가입 화면** - 구글, 네이버, 카카오, 페이스북 OAuth 버튼
- **생일 입력 화면** - 사용자 생일 등록 (선택사항)

### 2. 탭 네비게이션 (4개 탭)
#### Tab 1: 홈 (Home)
- 현재 캐시 잔액 표시 (대형 숫자)
- 생일 선물 배너 (생일인 경우)
- 캐시 획득 방법 (신규/기존 사용자 50만원)
- 최근 거래 내역 미리보기

#### Tab 2: 캐시 (Cash)
- 캐시 잔액 상세 보기
- 캐시 획득 내역 (타임라인 형식)
- 캐시 사용 내역

#### Tab 3: 출금/기프트 (Withdraw)
- 출금 (계좌 입금)
- 기프트카드 구매
- 기프트콘 구매
- 각 옵션별 최소 금액 표시

#### Tab 4: 프로필 (Profile)
- 사용자 정보 (이름, 생일, 가입일)
- 계정 설정
- 로그아웃

## 주요 콘텐츠 및 기능

### 홈 화면 (Home)
- **헤더**: "CashBox" 로고, 알림 아이콘
- **캐시 카드**: 현재 잔액 (대형 폰트, 금색 강조)
- **생일 배너**: 생일인 경우 "🎂 생일 축하합니다! 60억 캐시 받으셨습니다"
- **액션 버튼**: 출금하기, 기프트 구매
- **최근 거래**: 스크롤 가능한 거래 내역 리스트

### 캐시 화면 (Cash)
- **캐시 잔액**: 상단에 고정
- **필터**: 전체, 획득, 사용 필터
- **타임라인**: 날짜별 캐시 거래 내역
  - 획득: +금액 (초록색)
  - 사용: -금액 (빨간색)

### 출금/기프트 화면 (Withdraw)
- **출금 섹션**
  - 계좌 등록 (은행, 계좌번호)
  - 출금 금액 입력
  - 최소 출금액: 10,000원
  - 출금 버튼

- **기프트카드 섹션**
  - 기프트카드 종류 (아마존, 애플, 구글플레이 등)
  - 금액 선택 (10,000원 이상)
  - 구매 버튼

- **기프트콘 섹션**
  - 기프트콘 종류 (GS25, CU, 이마트 등)
  - 금액 선택 (5,000원 이상)
  - 구매 버튼

### 프로필 화면 (Profile)
- **사용자 정보**
  - 프로필 이미지
  - 이름
  - 생일
  - 가입일
  - 총 획득 캐시
  - 총 사용 캐시

- **설정**
  - 계정 설정
  - 개인정보 수정
  - 로그아웃

## 주요 사용자 흐름 (Key User Flows)

### Flow 1: 신규 사용자 가입 및 캐시 획득
1. 로그인 화면에서 "구글/네이버/카카오/페이스북으로 시작하기" 버튼 클릭
2. OAuth 인증 완료
3. 생일 입력 (선택)
4. 홈 화면으로 이동
5. 신규 사용자 50만원 캐시 자동 지급 (팝업 표시)

### Flow 2: 생일 축하 선물 받기
1. 사용자 생일 날짜 도달
2. 앱 실행 시 생일 축하 배너 표시
3. "선물 받기" 버튼 클릭
4. 60억 캐시 지급 (개발자) 또는 추가 캐시 지급 (일반 사용자)
5. 축하 애니메이션 표시

### Flow 3: 캐시 출금하기
1. 출금/기프트 탭 이동
2. "출금" 섹션에서 계좌 등록 (첫 회차만)
3. 출금 금액 입력 (최소 10,000원)
4. "출금 신청" 버튼 클릭
5. 확인 팝업 표시
6. 출금 완료 (3-5일 내 계좌 입금)
7. 거래 내역에 기록

### Flow 4: 기프트카드 구매
1. 출금/기프트 탭 이동
2. "기프트카드" 섹션에서 종류 선택
3. 금액 선택 (10,000원 이상)
4. "구매" 버튼 클릭
5. 결제 확인
6. 기프트카드 코드 표시 (복사 가능)
7. 이메일로도 발송

### Flow 5: 기프트콘 구매
1. 출금/기프트 탭 이동
2. "기프트콘" 섹션에서 편의점 선택
3. 금액 선택 (5,000원 이상)
4. "구매" 버튼 클릭
5. 결제 확인
6. 기프트콘 번호 표시 (복사 가능)
7. SMS로도 발송

## 색상 선택 (Color Choices)

### 주요 색상
- **Primary (강조)**: #FFD700 (금색) - 캐시 강조
- **Background**: #FFFFFF (밝음) / #0F0F0F (어두움)
- **Surface**: #F5F5F5 (밝음) / #1A1A1A (어두움)
- **Text**: #1A1A1A (밝음) / #FFFFFF (어두움)
- **Success**: #4CAF50 (초록색) - 캐시 획득
- **Error**: #F44336 (빨간색) - 캐시 사용
- **Muted**: #999999 (회색) - 보조 텍스트

### 브랜드 느낌
- 틱톡 라이트의 밝고 활발한 느낌
- 금색을 통한 프리미엄한 캐시 강조
- 깔끔하고 현대적인 디자인

## 레이아웃 원칙
- **모바일 세로 방향 (9:16)** 최적화
- **한 손 사용 가능** - 하단 탭 네비게이션
- **빠른 피드백** - 버튼 클릭 시 즉시 반응
- **명확한 정보 계층** - 중요한 정보 먼저 표시
- **스크롤 최소화** - 한 화면에 주요 정보 표시
